{
  "name": "law-case-manager-root",
  "private": true,
  "scripts": {
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "dev": "npm run dev:server & npm run dev:client",
    "build": "npm run build --workspaces"
  },
  "workspaces": [
    "client",
    "server"
  ]
}