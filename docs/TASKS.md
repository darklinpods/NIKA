# 待办清单 (Tasks)

记录 NIKA 的功能规划、技术债务和待验证事项。每轮开发开始前先看本文件和 `docs/CURRENT.md`。

## 当前推荐下一步

### [P1] 手动验证证据管理重构主流程
- **描述**：证据读取、当事人提取、发票提取、证据整理和证据目录生成已完成代码重构，需要在真实页面流程中手动验证。
- **验收标准**：
  - 上传证据后只新增 `category='Evidence'` 的原始证据文档
  - 重扫案卷后前端案件状态完整刷新
  - 发票提取结果写入并显示自 `evidenceData`
  - 索赔试算能读取发票并写回 `evidenceData.claimsList`
  - 证据目录包含证据类型和证明目的
- **不做范围**：
  - 本轮不新增证据项持久化表
  - 本轮不迁移旧数据库中的 `caseFactSheet` 历史 JSON
  - 本轮不调整 UI 大布局
- **状态**：待验证

---

## 格式说明

- **[P0]**：阻塞性问题或核心正确性风险
- **[P1]**：高优先级，影响主要工作流
- **[P2]**：中优先级，可排期处理
- **[P3]**：低优先级，体验优化或锦上添花

任务状态：
- **待开始**：尚未进入开发
- **进行中**：正在实现或已有明显代码推进
- **待验证**：代码已完成，仍需构建、手测或业务复核
- **已完成**：实现、验证和文档同步均完成
- **暂缓**：短期不处理，但保留
- **放弃**：明确不再实施

---

## 功能增强

### [P1] 持久化证据整理结果
- **描述**：当前证据整理结果只作为接口返回，不会保存。若律师需要编辑提交顺序、证明目的、缺失证据状态，应增加持久化结构。
- **候选方案**：
  - 新增 `EvidenceItem` 表
  - 或将整理结果暂存到 `Case.evidenceData.organizedEvidence`
- **状态**：待调研

### [P1] 明确案件事实长期数据形态
- **描述**：`caseFactSheet` 当前回归为律师可读事实摘要，但旧结构化事实编辑器仍兼容 JSON。需要决定长期是否保留 Markdown、JSON 双形态。
- **验收标准**：
  - 明确字段职责
  - 更新前后端类型和 UI 入口
  - 更新 `docs/DECISIONS.md`
- **状态**：待调研

### [P1] 扩展案件类型 Skill
- **描述**：继续补齐买卖合同、不当得利、劳动争议等案件类型的 Skill 规则和模板映射。
- **涉及文件**：`skills/`、`server/src/skills/SkillLoader.ts`、`server/src/constants.ts`、`client/constants/caseTypes.ts`
- **状态**：待开始

### [P2] 证据目录导出为正式 Word/PDF
- **描述**：当前证据目录以 Markdown 表格生成，后续可导出为法院提交格式。
- **状态**：待开始

### [P2] 证据缺失项转任务
- **描述**：将高优先级缺失证据一键转为案件子任务。
- **状态**：待开始

---

## 优化改进

### [P1] AI 工具结果落库策略
- **描述**：当前部分工具只返回 Markdown，部分会写库。需要统一“工具何时写库、何时只返回”的规则。
- **状态**：待调研

### [P2] Prisma migration 管理
- **描述**：当前本地使用 `prisma db push`，若进入多人协作或部署环境，应建立迁移文件流程。
- **状态**：待开始

### [P2] 前端大 chunk 优化
- **描述**：`npm run build` 存在 Vite 大 chunk 警告，可对 PDF worker、Markdown 编辑器等模块做动态加载。
- **状态**：待开始

### [P2] 错误处理统一
- **描述**：前端多个面板仍使用 `alert()`，后端错误结构也不完全统一。
- **状态**：待开始

---

## Bug 修复

### [P0] 无

### [P2] 旧结构化事实编辑器与 Markdown caseFactSheet 的体验不一致
- **描述**：`client/components/factSheet/FactSheetEditor.tsx` 会尝试将 `caseFactSheet` 作为 JSON 解析。当前不会崩溃，但当 `caseFactSheet` 是 Markdown 时会显示空结构化表单。
- **状态**：待调研

---

## 技术债务

### 当前技术债务总览

| 优先级 | 债务 | 当前影响 | 建议处理顺序 |
| --- | --- | --- | --- |
| P1 | 证据管理重构主流程待手动验证 | 代码已通过构建，但真实页面流程尚未闭环确认 | 1 |
| P1 | 证据整理结果未持久化 | 证据排序、证明目的、缺失证据每次重新调用 AI，暂不能编辑复用 | 2 |
| P1 | `caseFactSheet` 长期形态未确定 | Markdown 摘要与旧结构化编辑器体验不一致 | 3 |
| P1 | AI 工具结果落库策略不统一 | 工具有的写库、有的只返回，后续 Agent 行为容易漂移 | 4 |
| P1 | 缺少关键服务测试 | 证据整理、派生数据解析、AI JSON 解析缺少回归保护 | 5 |
| P2 | Prisma migration 体系缺失 | 本地 `db push` 可用，但多人协作/部署风险较高 | 6 |
| P2 | 前端大 chunk 警告 | 构建通过，但首屏和加载性能有优化空间 | 7 |
| P2 | 错误处理不统一 | 多处 `alert()` 和后端错误格式不一致，体验与排查成本较高 | 8 |
| P2 | 前后端 Case 类型重复 | `client/types.ts` 与 `server/src/types.ts` 易漂移 | 9 |
| P2 | `any` 使用较多 | 核心边界类型不够收紧，重构时缺少类型保护 | 10 |

### [P1] 增加关键服务测试
- **描述**：项目暂无测试框架。优先为 `evidenceOrganizerService`、`evidenceData` helper、`aiJsonParser` 增加最小测试。
- **状态**：待开始

### [P2] 统一 Case 类型
- **描述**：`client/types.ts` 和 `server/src/types.ts` 存在重复定义，长期可抽取共享 schema 或生成类型。
- **状态**：待开始

### [P2] 收紧核心边界的 `any` 类型
- **描述**：Agent/tool 调用、`caseService.updateCase`、前端任务操作等位置仍大量使用 `any`。短期不做全量清理，优先收紧证据管理、Case 更新、AI 工具返回这些核心边界。
- **状态**：待开始

---

## 已完成任务

### 2026-06-03
- [x] [重构] 建立证据读取 helper，统一证据 AI 只读取原始证据
- [x] [修复] 修复重扫案卷接口返回契约，返回完整 `caseData`
- [x] [重构] 新增 `evidenceData`，拆清案件事实与机器派生证据数据
- [x] [重构] 抽取 `evidenceOrganizerService`，证据整理与证据目录共用逻辑
- [x] [文档] 建立 NIKA 项目 Wiki 六件套

---

*最后更新：2026-06-03*
